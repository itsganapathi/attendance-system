# Leave unset for local dev (uses the Vite proxy to http://localhost:5000).
# Set to your deployed backend URL when running against production, e.g:
# VITE_API_URL=https://your-backend.vercel.app/api
VITE_API_URL=
